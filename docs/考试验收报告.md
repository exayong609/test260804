# 考试最终验收报告

验收日期：2026-08-07

源码仓库：https://github.com/exayong609/test260804

推荐分支：`fix/main-exam-hardening`

推荐 Preview：https://test260804-git-fix-main-exam-hardening-exayong-1502s-projects.vercel.app/import-tasks

对比分支：`fix/upload-p95-observability`（不推荐）

## 评分口径

最新验收要求采用异步导入专项 100 分口径：异步事件驱动 20、批量处理与性能 25、幂等重试恢复 15、错误精细化 10、可观测性 20、容灾降级与容量 5、提交质量 5。原始 V2 HTML 标注总分 100，但明细 10 + 30 + 50 + 20 = 110；本报告不利用该矛盾加分。

## 两版本评分

| 考点 | 满分 | Hardening | P95 版本 | 依据 |
|---|---:|---:|---:|---|
| 异步事件驱动 | 20 | 20 | 10 | Hardening 任务 ID 670ms 返回、973ms 可见；P95 上传 1,403ms 且文件未持久化 |
| 批量处理与性能 | 25 | 25 | 0 | Hardening 10,000 行约 48s、10/10 批；P95 0/10 批、0 行 |
| 幂等重试恢复 | 15 | 15 | 6 | Hardening 重复上传返回已有完成任务并验证重试；P95 返回不可用 pending 任务 |
| 错误精细化 | 10 | 10 | 8 | Hardening 40 条错误精确匹配，分页、批次、错误码、建议和导出齐全；P95 fresh run 无错误数据 |
| 可观测性 | 20 | 20 | 10 | Hardening 5 分钟折线、四阶段 P50/P95/P99、慢批次、点击联动、Trace 多维检索和告警均有真实数据；P95 监控全 0 |
| 容灾降级与容量 | 5 | 5 | 2 | Hardening 线上真实触发 W001 与横幅后继续完成；P95 无批次可触发 |
| 提交质量 | 5 | 5 | 3 | Hardening 26 项自动化测试、报告和最终表格完整；P95 仅 5 项测试且保留大量生成物 |
| **原始分** | **100** | **100** | **39** | P95 同时触发“上传超过 1 秒最高 50 分”封顶 |

最终推荐：`fix/main-exam-hardening`，实测评分 **100/100**。`fix/upload-p95-observability` 实测 **39/100**，即使按较宽松代码审查口径也不得超过 50 分。

## Hardening 验收证据

- Vercel Preview 正常加载，Next.js App Router + TypeScript 构建通过。
- 规则下拉首次加载保持“请选择规则”，必须手动选择；切换任务立即清空旧批次、Trace 和错误详情。
- 10,000 行任务 `task_84598796-ef7e-4585-82e4-247d2400e84e`：9,970 成功、30 失败、10/10 批次、约 48 秒。
- E001 × 30、E004 × 10，与压测文件注入数据精确一致。
- 多维 Trace 支持 task_id、trace_id、文件名、批次、行号范围和错误码；检索条件位于结果上方。
- 导入任务和行级错误均为固定高度滚动区域，无数据时不坍塌；筛选、分页、表头和底部对齐在线验证通过。
- 监控 Tab 展示近 5 分钟吞吐折线、四阶段 P50/P95/P99、错误分布、慢批次 TOP 10、阈值告警；错误码点击后自动联动到对应任务与错误筛选。
- 受控降级任务 `task_06de8689-ec4a-4b80-906a-1c510abe31aa`：W001 × 2、2/2 行继续成功，降级横幅和建议均可见。
- 可选 `IMPORT_ALERT_WEBHOOK_URL` 支持钉钉 Markdown 通知；仅在降级或 BullMQ 最终失败时发送，3 秒超时且不阻断主链路。
- DeepSeek `deepseek-chat` Profile 已在线配置并完成连接与 AI 规则生成界面测试；API Key 不进入仓库。

## P95 版本扣分点

- 上传接口 1,403ms，超过 1 秒硬指标，触发最高 50 分封顶。
- 请求结束后的延迟文件持久化在 Vercel Serverless 中未完成，46.2 秒后仍 pending。
- 10,000 行任务 0/10 批、0 行处理，没有可用性能结果。
- 重复上传 5,669ms 后返回同一个缺文件的 pending 任务，幂等策略放大故障。
- Trace 只有 `ImportTaskCreated`；监控吞吐、阶段耗时、错误分布和慢批次均为 0。
- 自动化测试仅 5 项，未覆盖 Outbox 同事务、文件激活、恢复投递、幂等重消费、降级和权限保护等关键场景。

## 最终自动化验证

| 检查 | 结果 |
|---|---|
| `npm test` | 26/26 通过 |
| `npm run typecheck` | 通过 |
| `npm run build` | 通过 |
| `git diff --check` | 通过 |

完整批次数据与两个 Preview 的 fresh run 见 `docs/压测报告.md` 和 `reports/load-test-result.json`。
