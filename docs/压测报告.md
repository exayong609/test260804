# 线上压测与版本对比报告

测试日期：2026-08-07

本报告只使用 Vercel Preview、Neon PostgreSQL、Upstash Redis、Railway Worker 与真实 Chrome 界面数据，不使用本地 API 或模拟任务。

## 对比环境

| 版本 | 提交 | Vercel Preview |
|---|---|---|
| `fix/main-exam-hardening` | `79e8ed5` | https://test260804-git-fix-main-exam-hardening-exayong-1502s-projects.vercel.app/import-tasks |
| `fix/upload-p95-observability` | `30060de` | https://test260804-git-fix-upload-p95-obs-36aded-exayong-1502s-projects.vercel.app/import-tasks |

## Hardening 版本最新 10,000 行实测

文件：`final-audit-10000-20260807.xlsx`

任务：`task_097037a3-5865-4c6c-ad91-5f0757f4d92b`

Trace：`trace_c6cb4039-3ef8-4d30-8499-05e7b362ab40`

| 指标 | 真实结果 | 结论 |
|---|---:|---|
| 任务初始化服务端耗时 | 442ms | 通过 1 秒硬指标 |
| 浏览器收到任务 ID | 684ms | 通过 |
| 工具等待到 UI 通知 | 3,769ms | 包含浏览器自动化轮询；UI 自身记录 684ms |
| 后台原文件上传 | 10,776ms | 与任务初始化解耦，不阻塞任务 ID 返回 |
| 10,000 行端到端 | 约 56s | 通过 60 秒容量目标 |
| 处理结果 | 10,000 / 10,000 | 通过 |
| 成功 / 失败行 | 9,970 / 30 | 与注入数据一致 |
| 批次 | 10 / 10 | 通过 |
| 错误记录 | E001 × 30，E004 × 10，共 40 条 | 与预期精确一致 |
| 实时吞吐 | 9,970 行/分钟 | 有真实 5 分钟聚合值 |
| 队列积压 | 0 | 任务完成后归零 |

### 监控性能

| 阶段 | P50 | P95 | P99 |
|---|---:|---:|---:|
| 解析 | 949ms | 949ms | 949ms |
| 规则 | 594ms | 594ms | 594ms |
| 校验 | 1,095ms | 3,106ms | 3,886ms |
| 写入 | 3,441ms | 4,596ms | 4,694ms |

页面同时显示近 5 分钟吞吐折线、E001 30/E004 10/W001 2 错误分布、慢批次 TOP 10 和阈值告警。最慢可见批次为 `unit_0002`，校验 2,456ms、写入 4,719ms、总耗时 8,482ms；校验 P99 超过 3,000ms 时站内告警可见。

### 幂等、检索与恢复

- 重复上传同一文件 1,497ms 返回已有完成任务，提示“相同文件已导入，返回已有任务”，没有重复写入。
- Trace 以文件名 `final-audit-10000`、错误码 E004、行号 9990–9990 检索，返回 1 个任务、13 个事件、1 条精确行错误；关联错误表也只显示该行。
- 监控 E004 点击后联动到对应任务，行级错误表精确显示 10 条 E004。
- 历史失败批次通过 `/retry` 重跑后进度没有重复累计；Outbox、批次消费和运单业务键均有幂等约束。

### 线上受控降级

测试任务：`task_06de8689-ec4a-4b80-906a-1c510abe31aa`

Trace：`trace_fc271301-e90e-4d26-8ac1-2843e67c03b0`

在 Neon 对 `sku_master` 加 45 秒临时排他锁，不修改主数据。批次查询触发 3 秒超时后任务继续完成：2/2 行成功、0 失败、1/1 批次；界面显示“SKU 校验已降级”横幅并记录 W001 × 2。监控同步显示校验 P99 4,081ms、慢批次 7,954ms 和可点击的 W001 分布。数据库锁已自动释放。

## 原始 V2 UI 实测

### 1,000 行性能

使用 `1000-orders-ui.xlsx`，手动选择“内置｜汇总明细表：逐行收货字段”规则：

| 次数 | 上传到 1,000 行完整可见 | 结果 |
|---|---:|---|
| 冷启动 | 3,216ms | 小于 10 秒总时限 |
| 热运行 | 2,164ms | 同时小于 10 秒总时限和 3 秒渲染时限 |

文件含 1 条故意错误：将第 999 行数量从 -1 改为 1 后错误清零、提交按钮恢复。分页/窗口化渲染、横向滚动、新增行、删除行和 Excel 导出均在 Chrome 界面实际操作完成。

### DeepSeek 与原始附件

- `deepseek-chat` Profile 连接测试 1,702ms 成功。
- AI 为 `湖南仓.xlsx` 生成 98% 置信度、10 个映射的推荐规则，预览 167 行、61 单、0 错误，保存为 `AI验收｜湖南仓自动规则`。
- 目录中现有 6 份原始附件全部通过 UI：湖南仓 167 行/61 单、海口 2 行/1 单、多 Sheet 21 行/3 单、矩阵 15 行、卡片 9 行/3 单、PDF 41 行/1 单，全部 0 错误，单次耗时 3,356–4,755ms。
- 原始考纲列出的 `门店配送确认单`、`周配送计划`、`配送签收单(多单PDF)` 文件不在提供目录中；代码有对应通用规则能力，但未拿原件实测，不能计为 9/9 通过。

### 提交持久化

Chrome 界面上传 `final-ui-submit.xlsx` 后解析为 1 行、1 单、0 错误；点击提交后返回“提交成功 1 单，失败 0 单”。历史记录首条可查：外部编码 `UI_FINAL_20260807_1048`、门店 `终验门店`、收件人 `终验用户`、电话 `13812345678`、1 个 SKU。

## P95 版本实测

任务：`task_7c55d9ec-db20-4bd4-bfba-5943aeaab7a0`

Trace：`trace_97f818a9-d6e4-4c80-bf98-806026e738aa`

| 指标 | 真实结果 | 结论 |
|---|---:|---|
| 上传接口 | 1,403ms | 未通过 1 秒硬指标 |
| UI 稳定观察 | 约 19.2s | 仍未开始处理 |
| 最长观察 | 46.2s | 仍为 pending |
| 批次 / 已处理 | 0/10；0/10,000 | 全链路失败 |
| Trace | 仅 `ImportTaskCreated` | 文件持久化未完成 |
| 重复上传 | 5,669ms | 返回同一个不可用 pending 任务 |
| 监控 | 吞吐、分位、错误分布均为 0 | 无可验收数据 |

该分支在 Vercel Serverless 中依赖请求结束后的延迟文件持久化，函数结束后工作没有可靠完成；重复检测又把未持久化文件的 pending 任务当成可复用结果。fresh run 可稳定复现，不应作为提交版本。
