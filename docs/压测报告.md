# 压测报告

状态：本地演示适配器已完成一次链路验证；真实 PostgreSQL、Redis 和线上部署仍需另行压测，不使用演示数据冒充生产结论。

| 项目 | 结果 |
|---|---|
| 测试时间 | 2026-08-04 |
| 部署环境 | 本地 Next.js + 内存任务适配器 |
| Worker 数量 / 并发 | 4 / 4（计划值） |
| 数据库 / 连接池 | PostgreSQL / 40（计划值） |
| SKU 主数据 | 20,000 条 |
| 压测文件 | 10,000 行 |
| 上传接口 | 831ms（单次链路验证，非 P95） |
| 任务总耗时 | 19,997ms（演示进度模型） |
| 处理单元 P50 / P95 | 待填写 |
| SKU 校验耗时 | 待填写 |
| 批量写入耗时 | 待填写 |
| 500 / 504 | 0 |

本次结果仅证明接口契约、轮询脚本和状态聚合能够闭环，不构成生产吞吐证明。正式报告必须在 Vercel + PostgreSQL + Redis + 常驻 Worker 环境中以多轮样本统计 P95/P99，并替换上表的演示数据。

压测命令：`APP_URL=https://实际地址 npm run loadtest`。报告提交时需附数据库连接数日志、监控看板截图、错误率、结论和已知瓶颈。
